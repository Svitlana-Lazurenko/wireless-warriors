import { save, load } from "./storage";
const ulEl = document.querySelector('.libraryList');
const STORAGE_KEY_LIBRARY = 'myLibrary';

window.addEventListener("DOMContentLoaded", fillMyLibrary);

function fillMyLibrary () {

}

function createMarkup () {
    
}